/*Cree una función para procesar una cadena de ADN (por ejemplo, 'CTAGGGTA'). 
- La función debe eliminar cualquier base de ADN no canónica 
(cualquier otra que no sea 'C', 'T', 'A', 'G'). 
- La función debe distinguir entre mayúsculas y minúsculas 
(mantenga 'C' pero elimine 'c'). 
- Si la cadena de ADN está vacía, devuelve una cadena vacía */

var cadenaAdn = 'CTAGGGTA'
function eliminarAdn (cadenaAdn) {
    if ( cadenaAdn != 'CTAGGGTA') {
        return eliminarAdn;
    }
}
function eliminarLetraAdn (cadenaAdn) {
    if ( eliminarLetraAdn !== 'c') {
        return 'C';
    }

}
function procesarAdn (cadenaAdn) {
    if (cadenaAdn ()) {
        return ();
    }
}


